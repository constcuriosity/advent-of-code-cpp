#ifndef H_AOC_STATE_ENUM
#define H_AOC_STATE_ENUM

//-------------- definitions

enum class e_aoc_states
{
	introduction,
	control_flow,
	select_year,
	select_day,
	execute_day,
	terminate,

	k_count
};

#endif // H_AOC_STATE_ENUM
